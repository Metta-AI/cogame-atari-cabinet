# The dependency paths are regenerated from the synced nimby package tree by
# the Dockerfile build stage and by ci.yml's "Regenerate nim.cfg" step: a
# committed absolute path is wrong on every host but the author's. Only
# repo-wide switches live here.
--path:"src"
